# GPT-5.6 Sol (`gpt-5.6-sol`)

The alternate codex lane, used when Brandon says so or the config's `codex_lane` names it. Same CLI, same flags, same brief shape as Astra; only the id in `lanes.toml` differs.

## Dated facts

- The tier-gating 400 was first seen on this id (2026-07-12; `codex-cli.md`).
- Effort `high` is the starting point; "a higher effort spread to its subagents and burnt tokens" is carried from the old notes UNDATED and unmeasured since.
- Fast tier on Sol: the catalog says 1.5x speed (read 2026-09-22), unmeasured here.
- The old notes' Sol brief guidance (XML-style tags, the strike rule, the final check) survives only as the shared brief shape in `templates/brief.md`.

## Unmeasured

Everything the rethink measured on Astra: continuity across resumes, round times, token use per round.
