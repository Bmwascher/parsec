# Design insert

The subject is the two files in the package, `spec.md` and `tasks.md`. Check the task list against the spec, and every fenced block against the code it names in the tree: a task whose code cannot work where it lands is a finding. Check the file map, the consumes and produces chain, and the three self-review points of each template.
