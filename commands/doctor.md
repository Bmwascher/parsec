---
description: Run the parsec doctor in the background and show its table; `/doctor update` runs the updates.
---

Run `python "${CLAUDE_PLUGIN_ROOT}/tools/parsec.py" doctor` in the background, named `Doctor`, with the arguments given ($ARGUMENTS); `update` means `--update`. Show its Markdown output as is, outside a code block. Everything else is in the tool's `--help`.
